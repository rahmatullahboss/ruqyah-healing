---
name: problem-fix
description: "Problem Fix for ruqyah-healing. 8 error→fix pairs."
domain: problem-fix
composesFrom:
  - fix
triggers:
  - glob: "**/*"
autoGenerated: true
enabled: true
---

# Problem Fix

Auto-compiled from **1 real patterns** in **ruqyah-healing**. This skill is auto-routed to agents when working on problem-fix files.

## 🔧 Problem Playbooks

### Known Error → Fix

| Error Pattern | Fix | Seen |
|-------|-----|------|
| `-       return new Response(JSON.stringify({ error` | Fixed null crash in APIRoute — protects against XSS and CSRF | 4x |
| `- - Fixed null crash in HTMLFormElement` | problem-fix in agent-rules.md | 4x |
| `yarn-error.log*` | problem-fix in .gitignore | 3x |
| `+       if (!r.ok) throw new Error('api fail');` | Fixed null crash in PrayerTimes — prevents null/undefined ru | 3x |
| `-       if (!r.ok) throw new Error('api fail');` | Fixed null crash in Left — prevents null/undefined runtime c | 2x |
| `+ export function isWorkersAiFreeLimitError(error)` | Fixed null crash in Access — hardens HTTP security headers | 2x |
| `- export function isWorkersAiFreeLimitError(error)` | Fixed null crash in Origin — hardens HTTP security headers | 2x |
| `-     } catch (error) {` | Fixed null crash in Chat — offloads heavy computation off th | 2x |

## 📐 Conventions & Best Practices

### Compiled Rules
- ✅ When encountering this, fix by: problem-fix in .gitignore (seen 2x)

---
*Auto-generated by BrainSync 🧠 | 1 patterns | 2026-04-14*
