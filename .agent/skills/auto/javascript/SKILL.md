---
name: javascript
description: "Javascript for ruqyah-healing. 3 gotchas, 6 conventions, 12 fixes."
domain: javascript
triggers:
  - glob: "**/*.js"
  - glob: "**/*.jsx"
autoGenerated: true
enabled: true
---

# Javascript

Auto-compiled from **34 real patterns** in **ruqyah-healing**. This skill is auto-routed to agents when working on javascript files.

## ⚠️ Anti-Patterns & Gotchas

> **CRITICAL:** These are real gotchas from this project. Ignoring them WILL cause bugs.

| ❌ Don't | Details |
|----------|----------|
| ⚠️ GOTCHA: Fixed null crash in Ruqyah — hardens HT | -  + import { createDb } from '../../db/client.js'; - const SYSTEM_PROMPT = `আপনি "Ruqyah Healing Ce |
| ⚠️ GOTCHA: Fixed null crash in Response — hardens  | - const corsHeaders = { + const chatRequestSchema = z.object({ -   'Access-Control-Allow-Origin': AL |
| ⚠️ GOTCHA: Fixed null crash in Response — hardens  | - function jsonResponse(payload, status = 200) { + function jsonResponse(payload, status = 200, requ |

## 🔧 Problem Playbooks

### Fixed null crash in Math — hardens HTTP security headers
-       result.choices?.[0]?.text,
+       result.choices?.[0]?.delta?.content,
-     ];
+       result.choices?.[0]?.text,
- 
+     ];
-     for (const candidate of candidateTexts) {
+ 
-       if (typeof candidate === 'string' && candidate.length > 0) {
+     for (const candidate of candidateTexts) {
-         return candidate;
+       if (typeof candidate === 'string' && candidate.length > 0) {

**Actionable Steps:**
1. Modified 1 files
2. identifier: Math
3. identifier: Date
4. identifier: Number
5. identifier: URL

### Fixed null crash in Date — hardens HTTP security headers
- async function getBudgetState(workerEnv, now = new Date()) {
+ function getOllamaFallbackConfig(workerEnv) {
-   if (!workerEnv?.AI_BUDGET?.get) {
+   const baseUrl = workerEnv?.OLLAMA_BASE_URL?.trim?.() || '';
-     return { usedNeurons: 0, key: getBudgetKey(now) };
+   if (!baseUrl) {
-   }
+     return null;
- 
+   }
-   const key = getBudgetKey(now);
+ 
-   const raw = await workerEnv.AI_BUD

**Actionable Steps:**
1. Modified 1 files
2. identifier: URL
3. identifier: Date
4. identifier: JSON
5. identifier: Number

### Fixed null crash in Chat — hardens HTTP security headers
- function buildPromptText(messages) {
+ async function getActiveSystemPrompt(workerEnv) {
-   return [{ role: 'system', content: SYSTEM_PROMPT }, ...messages]
+   try {
-     .map((message) => `${message.role}: ${message.content}`)
+     if (!workerEnv?.DATABASE_URL) return DEFAULT_SYSTEM_PROMPT;
-     .join('\n');
+     const db = createDb(workerEnv.DATABASE_URL);
- }
+     const [row] = await d

**Actionable Steps:**
1. Modified 1 files
2. identifier: Chat
3. identifier: Failed
4. identifier: JSON
5. identifier: DONE

### Fixed null crash in Boolean — prevents brute-force and DoS attacks
- import { env as workerEnv } from 'cloudflare:workers';
+ import { sequence } from 'astro:middleware';
- import { verifySession } from './lib/auth.js';
+ import { env as workerEnv } from 'cloudflare:workers';
- import { createDb } from './db/client.js';
+ import { verifySession } from './lib/auth.js';
- import { users } from './db/schema.js';
+ import { createDb } from './db/client.js';
- import 

**Actionable Steps:**
1. Modified 1 files
2. identifier: Set
3. identifier: Boolean
4. identifier: Rate
5. identifier: URL

### Fixed null crash in TextEncoder — hardens HTTP security headers
- export function createOpenAiCompatibleSseResponse(upstreamStream, options = {}) {
+ export function createOpenAiCompatibleSseResponse(upstreamStream, options = {}, request = null) {
-   const encoder = new TextEncoder();
+   const origin = request ? getCorsOrigin(request) : ALLOWED_ORIGINS[0];
-   const decoder = new TextDecoder();
+   const encoder = new TextEncoder();
-   const onComplete = ty

**Actionable Steps:**
1. Modified 1 files
2. identifier: TextEncoder
3. identifier: TextDecoder
4. identifier: ReadableStream
5. identifier: DONE

### Fixed null crash in TextEncoder — hardens HTTP security headers
- export function createSseResponseFromText(text) {
+ export function createSseResponseFromText(text, request = null) {
-   const encoder = new TextEncoder();
+   const origin = request ? getCorsOrigin(request) : ALLOWED_ORIGINS[0];
-   const ssePayload = [
+   const encoder = new TextEncoder();
-     toOpenAiDeltaEvent(text).trimEnd(),
+   const ssePayload = [
-     '',
+     toOpenAiDeltaEvent(t

**Actionable Steps:**
1. Modified 1 files
2. identifier: TextEncoder
3. identifier: DONE
4. identifier: Response
5. identifier: Access

### Fixed null crash in Origin — hardens HTTP security headers
- const corsHeaders = {
+ const ALLOWED_ORIGINS = [
-   'Access-Control-Allow-Origin': '*',
+   'https://ruqyah-healing.pages.dev',
-   'Access-Control-Allow-Methods': 'POST, OPTIONS',
+   'https://ruqyahhealing.com',
-   'Access-Control-Allow-Headers': 'Content-Type',
+ ];
- };
+ 
- 
+ function getCorsOrigin(request) {
- const chatRequestSchema = z.object({
+   const origin = request?.headers?.ge

**Actionable Steps:**
1. Modified 1 files
2. identifier: Origin
3. identifier: Allow
4. identifier: Access
5. identifier: Control

### Fixed null crash in Error — uses a proper password hashing algorithm
- // Define a simple fallback secret for local dev if env not setup
+ const getSecretKey = (env) => {
- // Always use a strong JWT_SECRET in production (.dev.vars or Wrangler Dashboard)
+   const [REDACTED]
- const getSecretKey = (env) => {
+   if (!secret) {
-   const [REDACTED] || 'super-secret-local-development-key';
+     throw new Error('JWT_SECRET environment variable is required. Set it in 

**Actionable Steps:**
1. Modified 1 files
2. identifier: Error
3. identifier: Set
4. identifier: Wrangler
5. identifier: Dashboard

### Fixed null crash in Access — hardens HTTP security headers
- const SECONDARY_MODEL = '@cf/moonshotai/kimi-k2.5';
+ const SECONDARY_MODEL = '@cf/google/gemma-3-12b-it';
- const FALLBACK_MODEL = '@cf/google/gemma-3-12b-it';
+ const MAX_OUTPUT_TOKENS = 450;
- const MAX_OUTPUT_TOKENS = 450;
+ const DEFAULT_DAILY_NEURON_LIMIT = 10_000;
- const DEFAULT_DAILY_NEURON_LIMIT = 10_000;
+ const DEFAULT_NEURON_RATES = {
- const DEFAULT_NEURON_RATES = {
+   inputPerMil

**Actionable Steps:**
1. Modified 1 files
2. identifier: Access
3. identifier: Control
4. identifier: Allow
5. identifier: Origin

### Fixed null crash in Access — hardens HTTP security headers
- const SECONDARY_MODEL = '@cf/zai-org/glm-4.7-flash';
+ const SECONDARY_MODEL = '@cf/moonshotai/kimi-k2.5';
- const TERTIARY_MODEL = '@cf/moonshotai/kimi-k2.5';
+ const FALLBACK_MODEL = '@cf/google/gemma-3-12b-it';
- const FALLBACK_MODEL = '@cf/google/gemma-3-12b-it';
+ const MAX_OUTPUT_TOKENS = 450;
- const MAX_OUTPUT_TOKENS = 450;
+ const DEFAULT_DAILY_NEURON_LIMIT = 10_000;
- const DEFAULT_DAI

**Actionable Steps:**
1. Modified 1 files
2. identifier: Access
3. identifier: Control
4. identifier: Allow
5. identifier: Origin

### Fixed null crash in Access — hardens HTTP security headers
- const SECONDARY_MODEL = '@cf/moonshotai/kimi-k2.5';
+ const SECONDARY_MODEL = '@cf/zai-org/glm-4.7-flash';
- const FALLBACK_MODEL = '@cf/google/gemma-3-12b-it';
+ const TERTIARY_MODEL = '@cf/moonshotai/kimi-k2.5';
- const MAX_OUTPUT_TOKENS = 450;
+ const FALLBACK_MODEL = '@cf/google/gemma-3-12b-it';
- const DEFAULT_DAILY_NEURON_LIMIT = 10_000;
+ const MAX_OUTPUT_TOKENS = 450;
- const DEFAULT_NEU

**Actionable Steps:**
1. Modified 1 files
2. identifier: Access
3. identifier: Control
4. identifier: Allow
5. identifier: Origin

### Fixed null crash in Access — hardens HTTP security headers
- 
+ const MAX_OUTPUT_TOKENS = 450;
- const corsHeaders = {
+ const DEFAULT_DAILY_NEURON_LIMIT = 10_000;
-   'Access-Control-Allow-Origin': '*',
+ const DEFAULT_NEURON_RATES = {
-   'Access-Control-Allow-Methods': 'POST, OPTIONS',
+   inputPerMillion: 50_000,
-   'Access-Control-Allow-Headers': 'Content-Type',
+   outputPerMillion: 100_000,
- 
+ const MODEL_NEURON_RATES = {
- const chatRequestSche

**Actionable Steps:**
1. Modified 1 files
2. identifier: Access
3. identifier: Control
4. identifier: Allow
5. identifier: Origin

## 📐 Conventions & Best Practices

### Project Conventions
- 📐 **Fixed null crash in Chat — offloads heavy computation off the main thread — confirmed 3x** — -         return createSseResponseFromText(text, request);
+         await logChatConversation(worke
- 📐 **what-changed in chat.js — confirmed 3x** — -           { role: 'system', content: SYSTEM_PROMPT },
+           { role: 'system', content: activ
- 📐 **Fixed null crash in Access — hardens HTTP security headers — confirmed 5x** — -       ...corsHeaders,
+       'Access-Control-Allow-Origin': origin,
-       'Content-Type': 'text
- 📐 **Fixed null crash in Access — hardens HTTP security headers — confirmed 4x** — -   [SECONDARY_MODEL]: DEFAULT_NEURON_RATES,
+   [SECONDARY_MODEL]: {
-   [SECONDARY_MODEL]: {
+    
- 📐 **what-changed in chat.js — confirmed 3x** — File updated (external): src/lib/ai/chat.js

Content summary (481 lines):
import { z } from 'zod';


- 📐 **Fixed null crash in Boolean — hardens HTTP security headers — confirmed 4x** — - function buildModelList(configuredModel) {
+ export function isWorkersAiFreeLimitError(error) {
- 

---
*Auto-generated by BrainSync 🧠 | 34 patterns | 2026-04-14*
