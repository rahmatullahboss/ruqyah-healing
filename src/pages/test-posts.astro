---
import { getCollection } from 'astro:content';
const posts = await getCollection('posts');
---
<html>
<body>
  <h1>Posts Count: {posts.length}</h1>
  <ul>
    {posts.map(p => (
      <li>ID: {p.id} | Title: {p.data.title} | Date: {p.data.date}</li>
    ))}
  </ul>
</body>
</html>
