---
import BaseLayout from '../layouts/BaseLayout.astro';
import Header from '../components/Header.astro';
import Hero from '../components/Hero.astro';
import Footer from '../components/Footer.astro';
import ServiceCard from '../components/ServiceCard.astro';
import BlogCard from '../components/BlogCard.astro';
import { env as workerEnv } from 'cloudflare:workers';
import { createDb } from '../db/client.js';
import { getAllPosts } from '../lib/posts.js';
import { courseLessons, courseModules, courses, testimonials } from '../db/schema.js';
import { clinicNumbers, clinicSchedule, getWhatsappUrl, primaryPhoneContact } from '../data/clinic.js';
import { buildFaqSchema } from '../lib/seo.js';
import { desc, eq, inArray } from 'drizzle-orm';
import { filterPublicReadyCourses, groupCourseContentRows } from '../lib/lms-admin.js';
import { demoCourses as demoCourseCatalog } from '../data/demo-courses.js';

export const prerender = false;

const env = workerEnv || process.env;
const db = createDb(env.DATABASE_URL);

const allPosts = await getAllPosts(db);

const publishedCourses = await db.select()
  .from(courses)
  .where(eq(courses.status, 'published'))
  .orderBy(desc(courses.createdAt));
const publishedCourseIds = publishedCourses.map((course) => course.id);
const [publishedModules, publishedLessons] = publishedCourseIds.length > 0
  ? await Promise.all([
      db.select().from(courseModules).where(inArray(courseModules.courseId, publishedCourseIds)),
      db.select().from(courseLessons).where(inArray(courseLessons.courseId, publishedCourseIds)),
    ])
  : [[], []];
const liveCoursesList = filterPublicReadyCourses(groupCourseContentRows({
  courses: publishedCourses,
  modules: publishedModules,
  lessons: publishedLessons,
})).map((row) => row.course).slice(0, 8);
const formatBnNumber = (value) => new Intl.NumberFormat('bn-BD').format(value);
const getCoursePreviewPrice = (course) => {
  const basePrice = Number(course.price || 0);
  const salePrice = Number(course.salePrice || 0);
  const effectivePrice = salePrice > 0 && salePrice < basePrice ? salePrice : basePrice;
  return effectivePrice > 0 ? `৳${formatBnNumber(effectivePrice)}` : 'ফ্রি';
};


const livePreviewCourses = liveCoursesList.slice(0, 4);
const livePreviewCourseIds = new Set(livePreviewCourses.map((course) => course.id));
const homepageDemoCourses = demoCourseCatalog.slice(0, 4)
  .filter((course) => !livePreviewCourseIds.has(course.id))
  .map((course) => ({
    ...course,
    href: '/courses',
  }));
const coursesList = [...livePreviewCourses, ...homepageDemoCourses].slice(0, 4);
const testimonialList = await db.select().from(testimonials).where(eq(testimonials.published, true)).orderBy(desc(testimonials.orderIndex)).limit(6);
const blogs = allPosts
  .slice(0, 6)
  .map((post) => ({
    date: new Date(post.date).toLocaleDateString('bn-BD', { year: 'numeric', month: 'long', day: 'numeric' }),
    category: post.category ?? 'রুকইয়াহ',
    title: post.title,
    excerpt: post.excerpt ?? '',
    href: `/posts/${post.slug}`,
  }));

const services = [
  {
    title: 'রুকইয়াহ',
    description: 'কুরআন ও সুন্নাহ ভিত্তিক শিরকমুক্ত রুকইয়াহ সেশন, প্রাথমিক ডায়াগনোসিস ও follow-up guidance।',
    link: '/ruqyah-diagnosis',
    image: '/images/ruqyah-info.webp',
    imageAlt: 'বাস্তবধর্মী রুকইয়াহ সেশন ও কুরআন তিলাওয়াতের পরিবেশ',
    bgColor: 'var(--primary-light)',
    iconColor: 'var(--primary)',
    icon: `<path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/>`,
  },
  {
    title: 'হিজামা',
    description: 'সুন্নাহসম্মত wet, dry ও combination cupping—পরিষ্কার, নিরাপদ ও প্রয়োজনভিত্তিক সেশন।',
    link: '/appointment',
    image: '/images/hijama-main.webp',
    imageAlt: 'বাস্তবধর্মী হিজামা কাপিং থেরাপি সেশন',
    bgColor: '#FFF3E0',
    iconColor: '#F57C00',
    icon: `<path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/>`,
  },
  {
    title: 'আকুপাংচার',
    description: 'ব্যথা, স্ট্রেস ও supportive pain management-এর জন্য সেশনভিত্তিক আকুপাংচার সাপোর্ট।',
    link: '/appointment',
    image: '/images/hijama-secondary.webp',
    imageAlt: 'থেরাপি রুমে আকুপাংচার ও ওয়েলনেস সাপোর্টের পরিবেশ',
    bgColor: '#E8F5E9',
    iconColor: '#2E7D32',
    icon: `<polyline points="22 12 18 12 15 21 9 3 6 12 2 12"/>`,
  },
  {
    title: 'লক্ষণ যাচাই',
    description: 'আপনার লক্ষণ, ইতিহাস ও বর্তমান অবস্থা বুঝে সঠিক সেবা বা পরামর্শের দিকনির্দেশনা।',
    link: '/symptom-diagnosis',
    image: '/images/gallery-session.webp',
    imageAlt: 'রোগীর সমস্যা শুনে লক্ষণ যাচাই ও কাউন্সেলিং সেশন',
    bgColor: 'var(--primary-light)',
    iconColor: 'var(--primary)',
    icon: `<path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"></path><polyline points="14 2 14 8 20 8"></polyline><line x1="16" y1="13" x2="8" y2="13"></line><line x1="16" y1="17" x2="8" y2="17"></line>`,
  },
  {
    title: 'রুকইয়াহ ডায়াগনোসিস',
    description: 'ধাপে ধাপে প্রশ্ন, লক্ষণ বিশ্লেষণ ও নিরাপদ রুকইয়াহ রুটিন সাজানোর প্রক্রিয়া।',
    link: '/ruqyah-diagnosis',
    image: '/images/services/ruqyah-diagnosis.jpeg',
    imageAlt: 'কুরআন তিলাওয়াত ও রুকইয়াহ ডায়াগনোসিসের পরিবেশ',
    bgColor: '#E0F7FA',
    iconColor: '#0288D1',
    icon: `<polyline points="22 12 18 12 15 21 9 3 6 12 2 12"/>`,
  },
  {
    title: 'রুকইয়াহ সাপ্লিমেন্ট',
    description: 'রুকইয়াহ, হিজামা ও প্রাকৃতিক যত্নের জন্য নির্বাচিত হালাল সাপোর্ট আইটেম।',
    link: '/supplements',
    image: '/images/services/ruqyah-supplement.jpeg',
    imageAlt: 'রুকইয়াহ ও প্রাকৃতিক সাপ্লিমেন্টের নির্বাচিত উপকরণ',
    bgColor: 'var(--pink-light)',
    iconColor: '#E53E3E',
    icon: `<path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"/>`,
  },
  {
    title: 'রুকইয়াহ ডাউনলোড',
    description: 'সেলফ রুকইয়াহ, দোয়া ও প্রয়োজনীয় গাইড PDF আকারে সহজে ডাউনলোড করুন।',
    link: '/pdf',
    image: '/images/services/ruqyah-download.jpeg',
    imageAlt: 'রুকইয়াহ গাইড ও ইসলামিক রিসোর্স ডাউনলোডের চিত্র',
    bgColor: 'var(--purple-light)',
    iconColor: '#7C3AED',
    icon: `<path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/>`,
  },
  {
    title: 'কনসাল্টেশন',
    description: 'কাউন্সেলিং, ডায়াগনোসিস, সেশন প্ল্যান এবং follow-up support বুকিং।',
    link: '/appointment',
    image: '/images/services/consultation.jpeg',
    imageAlt: 'রাকীর সাথে কনসাল্টেশন ও ফলো-আপ পরামর্শ',
    bgColor: '#FFF8E1',
    iconColor: '#F9A825',
    icon: `<path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/>`,
  },
];


const packages = [
  {
    title: 'রুকইয়াহ',
    desc: 'কুরআন ও সুন্নাহ ভিত্তিক রুকইয়াহ চিকিৎসা সেবা। জিন, জাদু ও বদনজরের চিকিৎসায় বিশেষজ্ঞ।',
    link: '/appointment',
    icon: '☪️',
  },
  {
    title: 'হিজামা',
    desc: 'সুন্নাহ সম্মত হিজামা থেরাপি। শরীরের বিভিন্ন রোগ নিরাময়ে কার্যকর।',
    link: '/appointment',
    icon: '🩸',
  },
  {
    title: 'আকুপাংচার',
    desc: 'ইসলামী দৃষ্টিভঙ্গিতে আকুপাংচার চিকিৎসা। শারীরিক ব্যথা নিরসনে দক্ষ।',
    link: '/appointment',
    icon: '🌿',
  },
];

const videos = [
  { id: 'A60hb-x5VrE', title: 'রুকইয়াহ হিলিং ভিডিও', url: 'https://youtu.be/A60hb-x5VrE' },
  { id: 'NFFmFAZLOl8', title: 'রুকইয়াহ ও ইসলামিক হিলিং গাইড', url: 'https://youtu.be/NFFmFAZLOl8' },
  { id: 'AvsAw8F7Q6Y', title: 'রুকইয়াহ সেবা সম্পর্কে জানুন', url: 'https://youtu.be/AvsAw8F7Q6Y' },
  { id: 'kRoizWnFJns', title: 'রুকইয়াহ শর্টস ১', url: 'https://youtube.com/shorts/kRoizWnFJns' },
  { id: 'Fo0jUp-l36U', title: 'রুকইয়াহ শর্টস ২', url: 'https://youtube.com/shorts/Fo0jUp-l36U' },
  { id: 'otaWquodJ_M', title: 'রুকইয়াহ শর্টস ৩', url: 'https://youtube.com/shorts/otaWquodJ_M' },
];



const primaryPhone = primaryPhoneContact?.display || clinicNumbers[0]?.display || '';
const generalContactLink = getWhatsappUrl('আসসালামু আলাইকুম, সেবা সম্পর্কে জানতে চাই।');
const ruqyahBookingLink = getWhatsappUrl('আসসালামু আলাইকুম, আমি রুকইয়াহ সেবা বুক করতে চাই।');
const hijamaBookingLink = getWhatsappUrl('আসসালামু আলাইকুম, আমি হিজামা সেবা বুক করতে চাই।');
const acupunctureBookingLink = getWhatsappUrl('আসসালামু আলাইকুম, আমি আকুপাংচার সেবা বুক করতে চাই।');
const acupressureBookingLink = getWhatsappUrl('আসসালামু আলাইকুম, আমি আকুপ্রেসার / ম্যাসাজ সেবা বুক করতে চাই।');

const demoProducts = [
  {
    title: 'কালোজিরা তেল',
    category: 'Natural Oil',
    price: '৳৪৫০',
    note: 'প্রাকৃতিক যত্ন ও দৈনন্দিন ব্যবহারের জন্য',
    badge: 'Popular',
    icon: '🌿',
  },
  {
    title: 'খাঁটি মধু',
    category: 'Natural Food',
    price: '৳৬৫০',
    note: 'নির্বাচিত মানের প্রাকৃতিক মধু',
    badge: 'Demo',
    icon: '🍯',
  },
  {
    title: 'সিদর পাতা পাউডার',
    category: 'Herbal Support',
    price: '৳৩৫০',
    note: 'সেলফ কেয়ার রুটিনের সহায়ক আইটেম',
    badge: 'New',
    icon: '🍃',
  },
  {
    title: 'হিজামা কাপ সেট',
    category: 'Hijama Accessory',
    price: '৳১,২০০',
    note: 'ট্রেনিং ও প্র্যাকটিস সাপোর্টের জন্য ডেমো সেট',
    badge: 'Demo',
    icon: '🧰',
  },
];

// FAQ data for AI search visibility
const homepageFaq = [
  {
    question: 'রুকইয়াহ কী?',
    answer: 'রুকইয়াহ হলো কুরআনের আয়াত ও হাদিসে বর্ণিত দোয়ার মাধ্যমে আধ্যাত্মিক ও শারীরিক রোগের চিকিৎসা। এটি ইসলামে স্বীকৃত একটি হালাল চিকিৎসা পদ্ধতি যা জিনের আছর, জাদু, বদনজর ও মানসিক সমস্যার কার্যকর সমাধান করে।',
  },
  {
    question: 'রুকইয়াহ কি হালাল?',
    answer: 'হ্যাঁ, রুকইয়াহ সম্পূর্ণ হালাল যতক্ষণ তা কুরআন, সুন্নাহ ও জায়েজ দুআ দ্বারা করা হয় এবং তাতে কোনো শিরক বা বিদআত থাকে না। আমাদের সেন্টার সম্পূর্ণ শিরকমুক্ত পদ্ধতি অনুসরণ করে।',
  },
  {
    question: 'রুকইয়াহ চিকিৎসায় কী কী সমস্যার সমাধান হয়?',
    answer: 'রুকইয়াহ চিকিৎসায় শারীরিক রোগ (মাথাব্যথা, পেটের ব্যথা, অনিদ্রা), মানসিক অস্থিরতা (ভয়, উদ্বেগ, বিষণ্নতা), বদনজর, হাসদ, জাদুর প্রভাব, জিনের আছর, ওয়াসওয়াসা এবং বিয়ে, সন্তান ও রিজিকে বাধা দূর করা হয়।',
  },
  {
    question: 'হিজামা থেরাপি কী?',
    answer: 'হিজামা বা কাপিং থেরাপি হলো নবী মুহাম্মদ (সা.) কর্তৃক অনুমোদিত একটি চিকিৎসা পদ্ধতি। শরীরের নির্দিষ্ট স্থানে কাপ লাগিয়ে দূষিত রক্ত বের করে নেওয়া হয়। এটি ব্যথা ও প্রদাহ কমায়, রক্ত সঞ্চালন উন্নত করে এবং রোগ প্রতিরোধ ক্ষমতা বাড়ায়।',
  },
  {
    question: 'অ্যাপয়েন্টমেন্ট কীভাবে নেওয়া যায়?',
    answer: 'আমাদের ওয়েবসাইটের /appointment পেজে ফর্ম পূরণ করে অ্যাপয়েন্টমেন্ট নেওয়া যায়। বুকিং ফি ৫০০ টাকা (অগ্রিম, ফেরতযোগ্য নয়) বিকাশ, নগদ বা ব্যাংকের মাধ্যমে পরিশোধ করতে হয়। পেমেন্ট যাচাইয়ের পর প্রতিনিধি ফোন বা SMS-এর মাধ্যমে শিডিউল চূড়ান্ত করবেন।',
  },
  {
    question: 'রুকইয়াহ সেশনে কী কী শর্ত মেনে চলতে হয়?',
    answer: 'রুকইয়াহ সেশনে পূর্ণ তাওহীদের উপর নির্ভরতা রাখা জরুরি। মহিলা রোগীর জন্য মাহরাম উপস্থিতি বাধ্যতামূলক। পৃথক সেশন, পূর্ণ পর্দা ও সম্মান বজায় রাখা হয়। কোনো ১০০% চিকিৎসার গ্যারান্টি দেওয়া হয় না; চূড়ান্ত শিফা আল্লাহর পক্ষ থেকে আসে।',
  },
  {
    question: 'আপনারা কি অনলাইন সেবা দেন?',
    answer: 'হ্যাঁ, আমাদের চেম্বার সেবা, হোম সার্ভিস এবং অনলাইন সেবা — তিনটি মাধ্যমেই সেবা প্রদান করা হয়। অনলাইন সেশনের মাধ্যমে দূর থেকেও রুকইয়াহ ডায়াগনোসিস ও কনসাল্টেশন নেওয়া সম্ভব।',
  },
];

const faqSchema = buildFaqSchema(homepageFaq);
---

<BaseLayout
  title="রুকইয়াহ হিলিং সেন্টার | Islamic Ruqyah Healing Center — Dhaka, Bangladesh"
  description="ইসলামী রুকইয়াহ চিকিৎসা কেন্দ্র — কুরআন ও সুন্নাহ ভিত্তিক আধ্যাত্মিক ও শারীরিক সুস্থতা। Shariah-compliant Ruqyah, Hijama & Acupuncture healing in Dhaka, Bangladesh."
  keywords={['রুকইয়াহ', 'ruqyah', 'islamic healing', 'ruqyah Dhaka', 'হিজামা', 'hijama', 'jinn treatment', 'black magic removal', 'evil eye cure', 'spiritual healing Bangladesh']}
>
  <Header />

  <main class="home-page">
    <!-- ① Hero -->
    <Hero />

    <!-- ①.৫ রুকইয়াহ কী? — What is Ruqyah? (Bilingual definition for AI search) -->
    <section class="section definition-section reveal">
      <div class="container">
        <div class="definition-grid">
          <div class="definition-bn scroll-reveal">
            <h2>রুকইয়াহ কী?</h2>
            <p class="definition-text">
              <strong>রুকইয়াহ</strong> হলো কুরআনের আয়াত ও হাদিসে বর্ণিত দোয়ার মাধ্যমে আধ্যাত্মিক ও শারীরিক রোগের চিকিৎসা। এটি ইসলামে স্বীকৃত একটি হালাল চিকিৎসা পদ্ধতি যা <em>জিনের আছর, জাদু, বদনজর</em> ও মানসিক সমস্যার কার্যকর সমাধান করে।
            </p>
          </div>
          <div class="definition-video scroll-reveal" style="--stagger-delay:100ms">
            <div class="definition-video-embed">
              <iframe
                src="https://www.youtube.com/embed/A60hb-x5VrE?rel=0"
                title="রুকইয়াহ হিলিং ভিডিও"
                allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
                allowfullscreen
                loading="lazy"
              ></iframe>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- ④ আমাদের সেবাসমূহ (8-card grid) -->
    <section class="section reveal">
      <div class="container">
        <div class="section-header scroll-reveal">
          <h2>আমাদের সেবাসমূহ</h2>
          <p>ইসলামিক পদ্ধতিতে আপনার সুস্থতার জন্য আমাদের বিশেষ সেবাসমূহ</p>
        </div>
        <div class="grid-4 services-grid">
          {services.map((service, i) => (
            <div class="scroll-reveal-child svc-wrap" style={`--stagger-delay: ${i * 60}ms`}>
              <ServiceCard {...service} />
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- ④ আমাদের ভিডিওসমূহ -->
    <section class="section videos-section reveal">
      <div class="container">
        <div class="section-header scroll-reveal">
          <h2>আমাদের ভিডিওসমূহ</h2>
          <p>রুকইয়াহ হিলিং সেন্টারের সচেতনতামূলক ভিডিও, গাইডলাইন ও সেশন পরিচিতি দেখুন</p>
        </div>
        <div class="video-grid">
          {videos.map((v, i) => (
            <article class="video-card scroll-reveal-child" style={`--stagger-delay:${i * 60}ms`}>
              <div class="video-embed">
                <iframe
                  src={`https://www.youtube.com/embed/${v.id}?rel=0`}
                  title={v.title}
                  loading="lazy"
                  allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
                  allowfullscreen
                ></iframe>
              </div>
              <a href={v.url} target="_blank" rel="noopener" class="video-title">{v.title}</a>
            </article>
          ))}
        </div>
      </div>
    </section>

    <!-- ⑤ রুকইয়াহ ব্লগ -->
    <section class="section reveal" style="background:var(--bg-light)">
      <div class="container">
        <div class="section-header-row scroll-reveal">
          <div>
            <h2>রুকইয়াহ ব্লগ</h2>
            <p>রুকইয়াহ, হিজামা ও ইসলামিক হিলিং নিয়ে সর্বশেষ নিবন্ধ এবং তথ্য</p>
          </div>
          <a href="/blog" class="btn btn-outline btn-sm">সব দেখুন</a>
        </div>
        <div class="blog-grid-desktop">
          {blogs.map((post: (typeof blogs)[number]) => (
            <div class="scroll-reveal-child"><BlogCard {...post} /></div>
          ))}
        </div>
        <div class="blog-swipe-mobile" id="blog-swipe">
          {blogs.map((post: (typeof blogs)[number]) => (
            <div class="blog-swipe-item"><BlogCard {...post} /></div>
          ))}
        </div>
        <div class="swipe-hint" aria-hidden="true"><span>← সোয়াইপ করুন →</span></div>
      </div>
    </section>

    <!-- ⑥ আমাদের প্যাকেজ -->
    <section class="section reveal">
      <div class="container">
        <div class="section-header scroll-reveal">
          <h2>আমাদের প্যাকেজ</h2>
          <p>চেম্বার, হোম ভিজিট বা অনলাইন—আপনার সুবিধামতো প্যাকেজ বেছে নিন</p>
        </div>
        <div class="pkg-grid">

          <!-- 1. রুকইয়াহ -->
          <div class="pkg-card-v2 scroll-reveal-child" style="--stagger-delay:0ms">
            <div class="pkg-thumb">
              <img src="/images/pkg-ruqyah.webp" alt="রুকইয়াহ প্যাকেজ" loading="lazy" width="600" height="600" />
              <span class="pkg-badge">☪️ রুকইয়াহ</span>
            </div>
            <div class="pkg-body">
              <h3>রুকইয়াহ চিকিৎসা</h3>
              <p>কুরআন ও সুন্নাহ ভিত্তিক আধ্যাত্মিক চিকিৎসা। জিন, জাদু ও বদনজরের কার্যকর সমাধান।</p>
              <div class="pkg-variants">
                <a href={ruqyahBookingLink} target="_blank" rel="noopener" class="pkg-btn pkg-btn--center">
                  🏢 চেম্বার
                </a>
                <a href="/appointment" class="pkg-btn pkg-btn--home">
                  📋 সব অপশন দেখুন
                </a>
                <a href={getWhatsappUrl('আসসালামু আলাইকুম, আমি রুকইয়াহ হোম ভিজিট সম্পর্কে জানতে চাই।')} target="_blank" rel="noopener" class="pkg-btn pkg-btn--home">
                  🏠 হোম ভিজিট
                </a>
              </div>
            </div>
          </div>

          <!-- 2. হিজামা -->
          <div class="pkg-card-v2 scroll-reveal-child" style="--stagger-delay:80ms">
            <div class="pkg-thumb">
              <img src="/images/pkg-hijama.webp" alt="হিজামা প্যাকেজ" loading="lazy" width="600" height="600" />
              <span class="pkg-badge">🩸 হিজামা</span>
            </div>
            <div class="pkg-body">
              <h3>হিজামা থেরাপি</h3>
              <p>সুন্নাহ সম্মত কাপিং থেরাপি। শারীরিক রোগ নিরাময়ে ইসলামিক পদ্ধতিতে কার্যকর চিকিৎসা।</p>
              <div class="pkg-variants">
                <a href={hijamaBookingLink} target="_blank" rel="noopener" class="pkg-btn pkg-btn--center">
                  🏢 চেম্বার
                </a>
                <a href="/appointment" class="pkg-btn pkg-btn--home">
                  📋 সব অপশন দেখুন
                </a>
                <a href={getWhatsappUrl('আসসালামু আলাইকুম, আমি হিজামা হোম ভিজিট সম্পর্কে জানতে চাই।')} target="_blank" rel="noopener" class="pkg-btn pkg-btn--home">
                  🏠 হোম ভিজিট
                </a>
              </div>
            </div>
          </div>

          <!-- 3. আকুপাংচার -->
          <div class="pkg-card-v2 scroll-reveal-child" style="--stagger-delay:160ms">
            <div class="pkg-thumb">
              <img src="/images/pkg-acupuncture.webp" alt="আকুপাংচার প্যাকেজ" loading="lazy" width="600" height="600" />
              <span class="pkg-badge">🌿 আকুপাংচার</span>
            </div>
            <div class="pkg-body">
              <h3>আকুপাংচার চিকিৎসা</h3>
              <p>ইসলামী দৃষ্টিভঙ্গিতে আকুপাংচার। শারীরিক ব্যথা, মাইগ্রেন ও দীর্ঘমেয়াদি সমস্যায় কার্যকর।</p>
              <div class="pkg-variants">
                <a href={acupunctureBookingLink} target="_blank" rel="noopener" class="pkg-btn pkg-btn--center">
                  🏢 চেম্বার
                </a>
                <a href="/appointment" class="pkg-btn pkg-btn--home">
                  📋 সব অপশন দেখুন
                </a>
                <a href={getWhatsappUrl('আসসালামু আলাইকুম, আমি আকুপাংচার হোম ভিজিট সম্পর্কে জানতে চাই।')} target="_blank" rel="noopener" class="pkg-btn pkg-btn--home">
                  🏠 হোম ভিজিট
                </a>
              </div>
            </div>
          </div>

          <!-- 4. আকুপ্রেশার -->
          <div class="pkg-card-v2 scroll-reveal-child" style="--stagger-delay:240ms">
            <div class="pkg-thumb">
              <img src="/images/pkg-acupressure.webp" alt="আকুপ্রেশার প্যাকেজ" loading="lazy" width="600" height="600" />
              <span class="pkg-badge">💆 আকুপ্রেসার</span>
            </div>
            <div class="pkg-body">
              <h3>আকুপ্রেসার ও ম্যাসাজ</h3>
              <p>প্রেশার পয়েন্ট ও থেরাপিউটিক ম্যাসাজের সমন্বয়ে স্ট্রেস, ক্লান্তি ও পেশির টান কমাতে সহায়ক।</p>
              <div class="pkg-variants">
                <a href={acupressureBookingLink} target="_blank" rel="noopener" class="pkg-btn pkg-btn--center">
                  🏢 চেম্বার
                </a>
                <a href="/appointment" class="pkg-btn pkg-btn--home">
                  📋 সব অপশন দেখুন
                </a>
                <a href={getWhatsappUrl('আসসালামু আলাইকুম, আমি আকুপ্রেসার বা ম্যাসাজ হোম ভিজিট সম্পর্কে জানতে চাই।')} target="_blank" rel="noopener" class="pkg-btn pkg-btn--home">
                  🏠 হোম ভিজিট
                </a>
              </div>
            </div>
          </div>

        </div>
      </div>
    </section>

    <!-- ⑦ আমাদের প্রোডাক্টসমূহ -->
    <section class="section product-section reveal">
      <div class="container">
        <div class="section-header scroll-reveal">
          <span class="section-kicker">Products</span>
          <h2>আমাদের প্রোডাক্টসমূহ</h2>
          <p>রুকইয়াহ, হিজামা ও প্রাকৃতিক যত্নের জন্য নির্বাচিত প্রোডাক্ট</p>
        </div>

        <div class="product-grid">
          {demoProducts.map((product, i) => (
            <article class="product-card scroll-reveal-child" style={`--stagger-delay:${i * 70}ms`}>
              <div class="product-media">
                <span class="product-icon" aria-hidden="true">{product.icon}</span>
                <span class="product-badge">{product.badge}</span>
              </div>
              <div class="product-body">
                <span class="product-category">{product.category}</span>
                <h3>{product.title}</h3>
                <p>{product.note}</p>
                <div class="product-footer">
                  <strong>{product.price}</strong>
                  <a href={getWhatsappUrl(`আসসালামু আলাইকুম, ${product.title} প্রোডাক্টটি সম্পর্কে জানতে চাই।`)} target="_blank" rel="noopener" class="product-order-btn">অর্ডার করুন</a>
                </div>
              </div>
            </article>
          ))}
        </div>

        <div class="product-cta-wrap">
          <a href="/supplements" class="btn btn-primary">সব প্রোডাক্ট দেখুন</a>
        </div>
      </div>
    </section>

    <!-- ⑧ আমাদের কোর্সসমূহ -->
    <section class="section courses-preview-section reveal">
      <div class="container">
        <div class="section-header scroll-reveal">
          <span class="section-kicker">Courses</span>
          <h2>আমাদের কোর্সসমূহ</h2>
          <p>রুকইয়াহ, হিজামা ও ওয়েলনেস সাপোর্ট নিয়ে structured, safe এবং practical learning path</p>
        </div>

        <div class="courses-preview-lead scroll-reveal">
          <span>ফ্রি প্রিভিউ</span>
          <span>বাংলা লেসন</span>
          <span>কুইজ ও সার্টিফিকেট</span>
          <span>Safety-first guidance</span>
        </div>
        
        <div class="courses-preview-grid">
          {coursesList.map((course, i) => {
            const courseSummary = course.shortDescription || course.desc || 'রুকইয়াহ ও ইসলামিক হিলিং শেখার জন্য সাজানো কোর্স।';
            const courseHref = course.href || `/courses/${course.id}`;
            return (
              <a href={courseHref} class="course-preview-card scroll-reveal-child" style={`--stagger-delay:${i * 70}ms`}>
                <div class="course-preview-img">
                  <img src={course.image} alt={course.title} loading="lazy" decoding="async" />
                  <span class="course-card-badge">{course.level}</span>
                  <span class:list={["course-price-chip", getCoursePreviewPrice(course) === 'ফ্রি' && 'free']}>
                    {getCoursePreviewPrice(course)}
                  </span>
                </div>
                <div class="course-preview-body">
                  <div class="course-card-meta">
                    <span>{course.category}</span>
                    <span>{course.classCount}</span>
                  </div>
                  <h3>{course.title}</h3>
                  <p>{courseSummary}</p>
                  <div class="course-mini-stats">
                    <span>{course.hours}</span>
                    <span>{course.language || 'বাংলা'}</span>
                  </div>
                  <div class="course-card-footer">
                    <strong>{getCoursePreviewPrice(course)}</strong>
                    <span class="course-view-more">বিস্তারিত দেখুন →</span>
                  </div>
                </div>
              </a>
            );
          })}
        </div>
        
        <div class="courses-cta-wrap">
          <a href="/courses" class="btn btn-outline">সকল কোর্স দেখুন</a>
        </div>
      </div>
    </section>

    <!-- Raqi Profile Preview -->
    <section class="section raqi-intro-section reveal">
      <div class="container">
        <div class="raqi-intro-card scroll-reveal">
          <div class="raqi-intro-photo-wrap">
            <img src="/images/raqi-omor.webp" alt="ওমর হাসান আজমি" class="raqi-intro-photo" loading="lazy" decoding="async" />
            <span class="raqi-photo-badge">রাকী</span>
          </div>
          <div class="raqi-intro-copy">
            <span class="section-kicker">রাকী পরিচয়</span>
            <h2>ওমর হাসান আজমি</h2>
            <p class="raqi-intro-lead">ইসলামী রুকইয়াহ চিকিৎসক, সুন্নাহভিত্তিক স্বাস্থ্যসেবা অনুশীলনকারী এবং রুকইয়াহ, হিজামা ও সাপোর্টিভ থেরাপি নিয়ে কাজ করেন।</p>
            <p>আল্লাহর অশেষ রহমতে দীর্ঘদিন ধরে কুরআন-সুন্নাহভিত্তিক শিরকমুক্ত রুকইয়াহ, প্রাথমিক ডায়াগনোসিস, সেলফ-রুকইয়াহ রুটিন এবং follow-up plan নিয়ে রোগীদের সাপোর্ট দিয়ে আসছেন। প্রতিটি সেশনে রোগীর সমস্যা শুনে লক্ষণ, history এবং current condition অনুযায়ী করণীয় সাজানো হয়।</p>
            <div class="raqi-intro-points">
              <span>☪️ শিরকমুক্ত রুকইয়াহ</span>
              <span>🩸 হিজামা সাপোর্ট</span>
              <span>📋 ডায়াগনোসিস ও ফলোআপ</span>
            </div>
            <div class="raqi-intro-actions">
              <a href="/team" class="btn btn-outline">আরো দেখুন</a>
              <a href={getWhatsappUrl('আসসালামু আলাইকুম, আমি রাকীর সাথে যোগাযোগ করতে চাই।')} target="_blank" rel="noopener" class="btn btn-primary">যোগাযোগ করুন</a>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- হোম সার্ভিস উপলব্ধ -->
    <section class="section home-service-section reveal">
      <div class="container">
        <div class="home-service-content scroll-reveal">
          <h2>হোম সার্ভিস উপলব্ধ</h2>
          <p>আমরা আপনার বাড়িতে এসে রুকইয়াহ, হিজামা ও আকুপাংচার সেবা প্রদান করি। সময় ও সুবিধামতো আপনার দরজায় পৌঁছে যাচ্ছে ইসলামিক চিকিৎসা।</p>
          <a href="/appointment" class="btn btn-primary">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="4" width="18" height="18" rx="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg>
            এখনই অ্যাপয়েন্টমেন্ট বুক করুন
          </a>
        </div>
      </div>
    </section>



    <!-- ⑫ সচরাচর জিজ্ঞাসা (FAQ) -->
    <section class="section reveal" style="background:var(--bg-light)">
      <div class="container">
        <div class="section-header scroll-reveal" style="text-align: center;">
          <h2>সচরাচর জিজ্ঞাসা</h2>
          <p>রুকইয়াহ ও আমাদের সেবা সম্পর্কে সাধারণ প্রশ্নাবলী</p>
        </div>
        <div class="faq-list">
          {homepageFaq.map((faq, i) => (
            <details class="faq-item scroll-reveal-child" style={`--stagger-delay: ${i * 60}ms`}>
              <summary class="faq-question">{faq.question}</summary>
              <div class="faq-answer">{faq.answer}</div>
            </details>
          ))}
        </div>
      </div>
    </section>
  </main>

  <script slot="head" type="application/ld+json" set:html={JSON.stringify(faqSchema)} />

  <Footer />
</BaseLayout>

<script>
  function initHomePage() {
    // ── Scroll-reveal
    const revealEls = document.querySelectorAll('.reveal');
    if ('IntersectionObserver' in window) {
      const observer = new IntersectionObserver(
        (entries) => {
          entries.forEach(entry => {
            if (entry.isIntersecting) {
              entry.target.classList.add('is-visible');
              observer.unobserve(entry.target);
            }
          });
        },
        { threshold: 0.07 }
      );
      revealEls.forEach(el => observer.observe(el));
    } else {
      revealEls.forEach(el => el.classList.add('is-visible'));
    }

    // ── Swipe hint
    const swipeHint = document.querySelector('.swipe-hint') as HTMLElement | null;
    if (swipeHint) {
      swipeHint.classList.remove('faded');
      setTimeout(() => swipeHint.classList.add('faded'), 2500);
    }

  }

  // ── Prayer Times (Aladhan API) ──────────────────────────
  async function loadPrayerTimes() {
    type PrayerTimings = Record<string, string>;
    type PrayerApiResponse = {
      data?: {
        timings?: PrayerTimings;
      };
    };
    type GeocodeResponse = {
      address?: {
        city?: string;
        town?: string;
        village?: string;
        country?: string;
      };
    };

    const set = (id: string, val: string) => {
      const el = document.getElementById(id);
      if (el) el.textContent = val;
    };

    const fmt = (t?: string) => {
      if (!t) return '—';
      const timeStr = t.split(' ')[0];
      const parts = timeStr.split(':');
      if (parts.length < 2) return t;
      let h = parseInt(parts[0], 10);
      let m = parseInt(parts[1], 10);
      if (isNaN(h) || isNaN(m)) return timeStr;
      const ampm = h >= 12 ? 'pm' : 'am';
      const h12 = h % 12 || 12;
      return `${h12}:${String(m).padStart(2,'0')} ${ampm}`;
    };

    const highlightNext = (timings: PrayerTimings) => {
      const keys = [
        ['Fajr','pt-fajr'],['Dhuhr','pt-dhuhr'],['Asr','pt-asr'],
        ['Maghrib','pt-maghrib'],['Isha','pt-isha']
      ] as const;
      const now = new Date();
      const nowMin = now.getHours() * 60 + now.getMinutes();
      let nextKey: (typeof keys)[number][0] = keys[0][0];
      for (const [k] of keys) {
        if (!timings[k]) continue;
        const timeStr = timings[k].split(' ')[0];
        const parts = timeStr.split(':');
        if (parts.length < 2) continue;
        let h = parseInt(parts[0], 10);
        let m = parseInt(parts[1], 10);
        if (h * 60 + m > nowMin) { nextKey = k; break; }
      }
      document.querySelectorAll('.pw-prayer-item').forEach(el => el.classList.remove('next-prayer'));
      const nextEl = document.querySelector(`.pw-prayer-item[data-prayer="${nextKey}"]`);
      if (nextEl) nextEl.classList.add('next-prayer');
    };

    const apiFetch = async (lat: number, lng: number) => {
      const url = `https://api.aladhan.com/v1/timings?latitude=${lat}&longitude=${lng}&method=1`;
      const r = await fetch(url);
      if (!r.ok) throw new Error('api fail');
      const data = (await r.json()) as PrayerApiResponse;
      return data.data;
    };

    const geocode = async (lat: number, lng: number) => {
      try {
        const r = await fetch(`https://nominatim.openstreetmap.org/reverse?lat=${lat}&lon=${lng}&format=json`);
        const d = (await r.json()) as GeocodeResponse;
        const city = d.address?.city || d.address?.town || d.address?.village || 'আপনার অবস্থান';
        const country = d.address?.country || '';
        const cityEl = document.getElementById('pt-city');
        const countryEl = document.getElementById('pt-country');
        if (cityEl) cityEl.textContent = city + ',';
        if (countryEl) countryEl.textContent = country;
      } catch (err) {
        // Ignore reverse geocoding errors
      }
    };

    const render = (data?: { timings?: PrayerTimings }) => {
      if (!data || !data.timings) return;
      const t = data.timings;
      set('pt-sunrise', fmt(t.Sunrise));
      set('pt-fajr',    fmt(t.Fajr));
      set('pt-dhuhr',   fmt(t.Dhuhr));
      set('pt-asr',     fmt(t.Asr));
      set('pt-maghrib', fmt(t.Maghrib));
      set('pt-isha',    fmt(t.Isha));
      highlightNext(t);
    };

    // Load default location (Dhaka) immediately to avoid empty state
    try {
      const defaultData = await apiFetch(23.8103, 90.4125);
      render(defaultData);
    } catch (err) {
      console.error('Aladhan API failed:', err);
    }

    // Attempt geolocation in background
    if ('geolocation' in navigator) {
      navigator.geolocation.getCurrentPosition(
        async ({ coords: { latitude: lat, longitude: lng } }) => {
          try {
            const data = await apiFetch(lat, lng);
            render(data);
            geocode(lat, lng);
          } catch (err) {}
        },
        () => {},
        { timeout: 6000 }
      );
    }
  }

  loadPrayerTimes();

  initHomePage();
  document.addEventListener('astro:page-load', initHomePage);

</script>

<style>
/* ── Homepage center alignment overrides ── */
:global(.home-page) {
  text-align: center;
}
:global(.home-page :is(h1, h2, h3, h4, p, li, span, summary, a, button)) {
  text-align: center;
}
:global(.home-page :is(.section-header, .section-header-row, .hero-content, .hero-copy, .info-text, .pkg-body, .course-body, .resource-card, .dark-card, .faq-answer, .definition-bn, .definition-en, .definition-video, .testimonial-card, .blog-card, .service-card)) {
  text-align: center !important;
}
:global(.home-page :is(.hero-actions, .section-header-row, .info-bullets, .cta-row, .pkg-variants, .phone-actions, .contact-actions)) {
  justify-content: center !important;
}
:global(.home-page :is(.hero-copy, .hero-desc, .section-header, .section-header-row, .info-text, .definition-text, .faq-answer)) {
  margin-left: auto;
  margin-right: auto;
}
:global(.home-page .section-header-row) {
  flex-direction: column;
  align-items: center !important;
  gap: 16px;
}
:global(.home-page .info-bullets) {
  align-items: center;
  padding-left: 0;
}
:global(.home-page .info-bullets li) {
  padding-left: 0;
}
:global(.home-page .info-bullets li::before) {
  position: static;
  margin-right: 6px;
}
:global(.home-page :is(.testimonial-header, .resource-card-icon)) {
  justify-content: center;
  margin-left: auto;
  margin-right: auto;
}


/* ── Raqi intro preview ── */
.raqi-intro-section {
  background: linear-gradient(180deg, #ffffff 0%, #f7fffb 100%);
}
.raqi-intro-card {
  display: grid;
  grid-template-columns: 220px 1fr;
  gap: 28px;
  align-items: center;
  max-width: 980px;
  margin: 0 auto;
  padding: 26px;
  border: 1px solid rgba(16, 185, 129, 0.18);
  border-radius: 28px;
  background: rgba(255, 255, 255, 0.92);
  box-shadow: 0 24px 60px rgba(6, 95, 70, 0.12);
  text-align: left;
}
.raqi-intro-photo-wrap {
  position: relative;
  width: 220px;
  max-width: 100%;
  margin: 0 auto;
}
.raqi-intro-photo {
  width: 100%;
  aspect-ratio: 1 / 1;
  object-fit: cover;
  border-radius: 24px;
  border: 6px solid #ecfdf5;
  box-shadow: 0 18px 34px rgba(6, 95, 70, 0.18);
}
.raqi-photo-badge {
  position: absolute;
  left: 50%;
  bottom: -12px;
  transform: translateX(-50%);
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 7px 16px;
  border-radius: 999px;
  background: var(--primary);
  color: #fff;
  font-size: 13px;
  font-weight: 800;
  box-shadow: 0 10px 24px rgba(16, 185, 129, 0.25);
}
.raqi-intro-copy {
  text-align: left !important;
}
.raqi-intro-copy h2 {
  margin: 6px 0 8px;
  font-size: clamp(24px, 3vw, 34px);
}
.raqi-intro-copy p {
  color: var(--text-muted);
  line-height: 1.8;
  text-align: left !important;
}
.raqi-intro-lead {
  color: var(--text-dark) !important;
  font-weight: 700;
}
.raqi-intro-points {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
  margin: 18px 0;
}
.raqi-intro-points span {
  display: inline-flex;
  align-items: center;
  padding: 8px 12px;
  border-radius: 999px;
  background: #ecfdf5;
  color: #065f46;
  font-weight: 700;
  font-size: 13px;
}
.raqi-intro-actions {
  display: flex;
  gap: 12px;
  flex-wrap: wrap;
}
@media (max-width: 760px) {
  .raqi-intro-card {
    grid-template-columns: 1fr;
    text-align: center;
    padding: 22px;
  }
  .raqi-intro-copy,
  .raqi-intro-copy p {
    text-align: center !important;
  }
  .raqi-intro-photo-wrap {
    width: 176px;
  }
  .raqi-intro-points,
  .raqi-intro-actions {
    justify-content: center;
  }
}

/* ── Courses Preview Section ── */
.courses-preview-section {
  padding: 88px 0;
  background:
    radial-gradient(circle at top left, rgba(17, 185, 129, 0.13), transparent 34%),
    radial-gradient(circle at bottom right, rgba(16, 185, 129, 0.11), transparent 30%),
    linear-gradient(135deg, #ffffff 0%, #f7fbf4 100%);
}

.courses-preview-lead {
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  justify-content: center;
  gap: 10px;
  margin: -6px auto 30px;
}

.courses-preview-lead span {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  padding: 9px 14px;
  border: 1px solid rgba(46, 125, 50, 0.14);
  border-radius: 999px;
  background: rgba(255, 255, 255, 0.74);
  color: var(--primary-dark, #155e3b);
  font-size: 13px;
  font-weight: 800;
  box-shadow: 0 12px 30px rgba(15, 68, 24, 0.06);
}

.courses-preview-lead span::before {
  content: '✓';
  display: inline-grid;
  place-items: center;
  width: 18px;
  height: 18px;
  border-radius: 50%;
  background: rgba(17, 185, 129, 0.12);
  color: var(--primary);
  font-size: 11px;
}

.courses-preview-grid {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 22px;
}

.course-preview-card {
  position: relative;
  display: flex;
  flex-direction: column;
  min-height: 100%;
  overflow: hidden;
  background: rgba(255, 255, 255, 0.96);
  border: 1px solid rgba(46, 125, 50, 0.14);
  border-radius: 28px;
  box-shadow: 0 18px 45px rgba(15, 68, 24, 0.08);
  text-decoration: none;
  color: inherit;
  isolation: isolate;
  transition: transform 0.25s ease, box-shadow 0.25s ease, border-color 0.25s ease;
}

.course-preview-card:hover {
  transform: translateY(-6px);
  border-color: rgba(46, 125, 50, 0.32);
  box-shadow: 0 24px 60px rgba(15, 68, 24, 0.14);
}

.course-preview-img {
  height: 172px;
  position: relative;
  overflow: hidden;
  background: linear-gradient(135deg, #eef7ef, #f8faf7);
}

.course-preview-img::after {
  content: '';
  position: absolute;
  inset: auto 0 0;
  height: 45%;
  background: linear-gradient(180deg, transparent, rgba(0, 0, 0, 0.42));
}

.course-preview-img img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: transform 0.35s ease;
}

.course-preview-card:hover .course-preview-img img {
  transform: scale(1.05);
}

.course-card-badge {
  position: absolute;
  left: 16px;
  bottom: 16px;
  z-index: 1;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  max-width: calc(100% - 32px);
  padding: 7px 12px;
  border-radius: 999px;
  background: rgba(255, 255, 255, 0.92);
  color: var(--primary-dark, #1b5e20);
  font-size: 12px;
  font-weight: 800;
  box-shadow: 0 10px 24px rgba(0, 0, 0, 0.12);
}

.course-price-chip {
  position: absolute;
  top: 14px;
  right: 14px;
  z-index: 1;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 7px 12px;
  border-radius: 999px;
  background: rgba(6, 95, 70, 0.9);
  color: #fff;
  font-size: 12px;
  font-weight: 900;
  box-shadow: 0 12px 28px rgba(6, 95, 70, 0.25);
}

.course-price-chip.free {
  background: rgba(20, 184, 166, 0.94);
}

.course-preview-body {
  display: flex;
  flex-direction: column;
  flex: 1;
  padding: 20px;
}

.course-card-meta {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 10px;
  margin-bottom: 12px;
  color: var(--text-muted);
  font-size: 12px;
  font-weight: 700;
}

.course-card-meta span {
  min-width: 0;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.course-preview-body h3 {
  font-size: 17px;
  font-weight: 900;
  margin-bottom: 10px;
  min-height: 48px;
  line-height: 1.4;
  color: var(--text-dark);
}

.course-preview-body p {
  display: -webkit-box;
  -webkit-line-clamp: 3;
  -webkit-box-orient: vertical;
  overflow: hidden;
  min-height: 66px;
  margin: 0 0 16px;
  color: var(--text-muted);
  font-size: 14px;
  line-height: 1.6;
}

.course-mini-stats {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  margin-bottom: 16px;
}

.course-mini-stats span {
  display: inline-flex;
  align-items: center;
  padding: 6px 10px;
  border-radius: 999px;
  background: rgba(46, 125, 50, 0.08);
  color: var(--primary-dark, #155e3b);
  font-size: 12px;
  font-weight: 800;
}

.course-card-footer {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
  margin-top: auto;
  padding-top: 16px;
  border-top: 1px solid rgba(46, 125, 50, 0.12);
}

.course-card-footer strong {
  color: var(--primary);
  font-size: 18px;
  font-weight: 900;
}

.course-view-more {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  padding: 9px 14px;
  border-radius: 999px;
  background: rgba(46, 125, 50, 0.1);
  color: var(--primary);
  font-size: 13px;
  font-weight: 800;
  transition: background 0.25s ease, color 0.25s ease;
}

.course-preview-card:hover .course-view-more {
  background: var(--primary);
  color: #fff;
}

.courses-cta-wrap {
  text-align: center;
  margin-top: 34px;
}

.courses-cta-wrap .btn-outline {
  display: inline-block;
  padding: 12px 32px;
  border: 2px solid var(--primary);
  border-radius: 24px;
  font-size: 15px;
  font-weight: 700;
  color: var(--primary);
  text-decoration: none;
  transition: all 0.2s;
}

.courses-cta-wrap .btn-outline:hover {
  background: var(--primary);
  color: #fff;
}

@media (max-width: 1180px) {
  .courses-preview-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
}

@media (max-width: 560px) {
  .courses-preview-section { padding: 64px 0; }
  .courses-preview-lead { justify-content: flex-start; }
  .courses-preview-grid { grid-template-columns: 1fr; }
  .course-preview-body h3 { min-height: auto; }
}


/* ── Product Section ── */
.product-section {
  background: linear-gradient(135deg, #f8faf7 0%, #eef7ef 100%);
}

.section-kicker {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 6px 14px;
  border-radius: 999px;
  background: rgba(46, 125, 50, 0.1);
  color: var(--primary);
  font-size: 12px;
  font-weight: 800;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  margin-bottom: 12px;
}

.product-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 22px;
}

.product-card {
  position: relative;
  overflow: hidden;
  background: #fff;
  border: 1px solid rgba(46, 125, 50, 0.14);
  border-radius: 24px;
  box-shadow: 0 18px 45px rgba(15, 68, 24, 0.08);
  transition: transform 0.25s ease, box-shadow 0.25s ease, border-color 0.25s ease;
}

.product-card:hover {
  transform: translateY(-6px);
  border-color: rgba(46, 125, 50, 0.3);
  box-shadow: 0 24px 60px rgba(15, 68, 24, 0.14);
}

.product-media {
  position: relative;
  min-height: 150px;
  display: flex;
  align-items: center;
  justify-content: center;
  background:
    radial-gradient(circle at 30% 20%, rgba(255,255,255,0.9), transparent 28%),
    linear-gradient(135deg, rgba(46, 125, 50, 0.16), rgba(217, 119, 6, 0.14));
}

.product-icon {
  font-size: 56px;
  filter: drop-shadow(0 12px 20px rgba(0,0,0,0.12));
}

.product-badge {
  position: absolute;
  top: 14px;
  right: 14px;
  padding: 5px 11px;
  border-radius: 999px;
  background: #fff;
  color: var(--primary);
  font-size: 11px;
  font-weight: 800;
  box-shadow: 0 8px 20px rgba(0,0,0,0.08);
}

.product-body {
  padding: 20px;
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.product-category {
  color: var(--primary);
  font-size: 12px;
  font-weight: 800;
  text-transform: uppercase;
  letter-spacing: 0.04em;
}

.product-body h3 {
  font-size: 19px;
  font-weight: 800;
  color: var(--text-dark);
}

.product-body p {
  min-height: 44px;
  color: var(--text-medium);
  font-size: 14px;
  line-height: 1.55;
}

.product-footer {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
  margin-top: 8px;
}

.product-footer strong {
  color: var(--text-dark);
  font-size: 20px;
  font-weight: 900;
}

.product-order-btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-height: 38px;
  padding: 9px 14px;
  border-radius: 999px;
  background: var(--primary);
  color: #fff;
  font-size: 13px;
  font-weight: 800;
  text-decoration: none;
  transition: transform 0.2s ease, background 0.2s ease;
}

.product-order-btn:hover {
  transform: translateY(-2px);
  background: var(--primary-dark);
}

.product-cta-wrap {
  text-align: center;
  margin-top: 32px;
}

@media (max-width: 1024px) {
  .product-grid { grid-template-columns: repeat(2, 1fr); }
}

@media (max-width: 560px) {
  .product-grid {
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 12px;
    align-items: start;
  }

  .product-card {
    border-radius: 18px;
    align-self: start;
  }
  .product-media { min-height: 92px; }
  .product-icon { font-size: 36px; }
  .product-badge {
    top: 8px;
    right: 8px;
    padding: 3px 7px;
    font-size: 9px;
  }
  .product-body {
    padding: 10px 10px 12px;
    gap: 5px;
  }
  .product-category {
    font-size: 9px;
    line-height: 1.2;
  }
  .product-body h3 {
    font-size: 14px;
    line-height: 1.25;
    margin: 0;
  }
  .product-body p {
    min-height: 0;
    margin: 0;
    font-size: 11px;
    line-height: 1.35;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
  .product-footer {
    flex-direction: column;
    align-items: stretch;
    gap: 6px;
    margin-top: 4px;
  }
  .product-footer strong {
    font-size: 15px;
    line-height: 1.1;
  }
  .product-order-btn {
    min-height: 31px;
    padding: 7px 8px;
    font-size: 11px;
  }
}

/* ── Testimonials Section ── */
.testimonials-section {
  padding: 64px 0;
  background: linear-gradient(135deg, #f8f7f4 0%, #f0efe9 100%);
}

.testimonials-section .section-title {
  text-align: center;
  font-size: clamp(24px, 4vw, 36px);
  font-weight: 800;
  margin-bottom: 8px;
}

.testimonials-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 24px;
}

.testimonial-card {
  background: #fff;
  border-radius: 16px;
  padding: 20px;
  box-shadow: 0 2px 12px rgba(0,0,0,0.06);
}

.testimonial-header {
  display: flex;
  align-items: center;
  gap: 12px;
  margin-bottom: 12px;
}

.testimonial-avatar {
  width: 48px;
  height: 48px;
  border-radius: 50%;
  background: var(--primary);
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
}

.testimonial-avatar img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.testimonial-avatar span {
  color: #fff;
  font-size: 18px;
  font-weight: 700;
}

.testimonial-info h4 {
  font-size: 15px;
  font-weight: 700;
  margin-bottom: 2px;
}

.testimonial-role {
  font-size: 12px;
  color: var(--text-muted);
}

.testimonial-rating {
  margin-bottom: 10px;
}

.testimonial-rating .star {
  color: #f59e0b;
  font-size: 14px;
}

.testimonial-message {
  font-size: 13px;
  color: var(--text-medium);
  line-height: 1.6;
}

@media (max-width: 900px) {
  .testimonials-grid { grid-template-columns: repeat(2, 1fr); }
}

@media (max-width: 560px) {
  .testimonials-grid { grid-template-columns: 1fr; }
}

/* ── Services grid — equal height cards ── */
.services-grid { align-items: stretch; }
.svc-wrap { display: flex; flex-direction: column; }

/* ── Info sections (রুকইয়াহ কি / হিজামা কি) ── */
.info-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 56px;
  align-items: center;
}
.info-grid--reverse { direction: rtl; }
.info-grid--reverse > * { direction: ltr; }

.info-text h2 {
  font-size: clamp(22px, 3.5vw, 34px);
  font-weight: 800;
  color: var(--text-dark);
  margin-bottom: 18px;
  line-height: 1.3;
}
.info-text p {
  font-size: 15px;
  color: var(--text-medium);
  line-height: 1.8;
  margin-bottom: 18px;
}
.info-bullets {
  list-style: none;
  padding: 0;
  margin: 0 0 24px;
  display: flex;
  flex-direction: column;
  gap: 10px;
}
.info-bullets li {
  font-size: 14px;
  color: var(--text-medium);
  padding-left: 22px;
  position: relative;
  line-height: 1.6;
}
.info-bullets li::before {
  content: '✔';
  position: absolute;
  left: 0;
  color: var(--primary);
  font-size: 12px;
  top: 2px;
}

.whatsapp-cta {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  background: #25D366;
  color: #fff;
  font-weight: 600;
  font-size: 14px;
  padding: 10px 20px;
  border-radius: 24px;
  text-decoration: none;
  transition: background 0.2s, transform 0.2s;
}
.whatsapp-cta:hover { background: #1ebe5a; transform: translateY(-2px); }

/* Video embed */
.video-placeholder {
  border-radius: var(--radius-lg);
  overflow: hidden;
  box-shadow: 0 8px 32px rgba(0,0,0,0.12);
  margin-bottom: 16px;
}
.video-placeholder iframe { display: block; }
.appt-btn { width: 100%; justify-content: center; margin-bottom: 10px; }
.appt-btn-outline { width: 100%; justify-content: center; }

/* Hijama images */
.hijama-images {
  position: relative;
  height: 340px;
}
.hijama-img {
  border-radius: var(--radius-lg);
  object-fit: cover;
  box-shadow: 0 8px 32px rgba(0,0,0,0.15);
}
.hijama-img--main {
  width: 75%;
  height: 100%;
  position: absolute;
  left: 0;
  top: 0;
}
.hijama-img--secondary {
  width: 45%;
  height: 55%;
  position: absolute;
  right: 0;
  bottom: 0;
  border: 4px solid #fff;
}

/* ── Video grid ── */
.videos-section {
  background: linear-gradient(180deg, rgba(246, 251, 248, 0.75), rgba(255, 255, 255, 0));
}
.video-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 20px;
}
.video-card {
  display: flex;
  flex-direction: column;
  gap: 12px;
  padding: 12px;
  background: #fff;
  border: 1px solid var(--border);
  border-radius: var(--radius-lg);
  box-shadow: 0 10px 28px rgba(15, 118, 110, 0.08);
}
.video-embed {
  position: relative;
  width: 100%;
  overflow: hidden;
  border-radius: var(--radius-md);
  aspect-ratio: 16 / 9;
  background: #000;
}
.video-embed iframe {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  border: 0;
}
.video-title {
  font-size: 14px;
  font-weight: 700;
  color: var(--text-dark);
  line-height: 1.4;
  text-decoration: none;
}
.video-title:hover { color: var(--primary); }



/* ── Package cards (old emoji style — kept as fallback) ── */
.pkg-card {
  background: #fff;
  border-radius: var(--radius-lg);
  padding: 36px 28px;
  text-align: center;
  border: 1px solid var(--border);
  box-shadow: 0 4px 20px rgba(0,0,0,0.06);
  display: flex;
  flex-direction: column;
  gap: 14px;
  transition: transform 0.3s ease, box-shadow 0.3s ease;
}
.pkg-card:hover { transform: translateY(-5px); box-shadow: 0 16px 40px rgba(0,0,0,0.12); }
.pkg-icon { font-size: 48px; line-height: 1; }
.pkg-card h3 { font-size: 22px; font-weight: 700; color: var(--text-dark); }
.pkg-card p { font-size: 14px; color: var(--text-medium); line-height: 1.7; flex: 1; }

/* ── Package v2 — image cards with Center/Home variants ── */
.pkg-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 20px;
}
.pkg-card-v2 {
  background: #fff;
  border-radius: var(--radius-lg);
  overflow: hidden;
  box-shadow: var(--shadow-md);
  display: flex;
  flex-direction: column;
  transition: transform 0.25s ease, box-shadow 0.25s ease;
}
.pkg-card-v2:hover { transform: translateY(-6px); box-shadow: var(--shadow-lg); }

.pkg-thumb { position: relative; overflow: hidden; aspect-ratio: 1/1; }
.pkg-thumb img { width: 100%; height: 100%; object-fit: cover; transition: transform 0.4s ease; }
.pkg-card-v2:hover .pkg-thumb img { transform: scale(1.06); }

.pkg-badge {
  position: absolute;
  bottom: 10px;
  left: 10px;
  background: rgba(0,0,0,0.55);
  backdrop-filter: blur(6px);
  color: #fff;
  font-size: 12px;
  font-weight: 700;
  padding: 4px 12px;
  border-radius: 20px;
}

.pkg-body { padding: 18px 16px 20px; display: flex; flex-direction: column; gap: 8px; flex: 1; }
.pkg-body h3 { font-size: 16px; font-weight: 700; color: var(--text-dark); }
.pkg-body > p { font-size: 13px; color: var(--text-medium); line-height: 1.6; flex: 1; }

.pkg-variants { display: flex; flex-direction: column; gap: 8px; margin-top: 12px; }
.pkg-btn {
  display: block;
  text-align: center;
  padding: 9px 12px;
  border-radius: 8px;
  font-size: 13px;
  font-weight: 700;
  text-decoration: none;
  transition: opacity 0.2s, transform 0.15s;
}
.pkg-btn:hover { opacity: 0.88; transform: translateY(-1px); }
.pkg-btn--center {
  background: var(--primary-light);
  color: var(--primary);
  border: 1.5px solid var(--primary);
}
.pkg-btn--home {
  background: var(--primary);
  color: #fff;
}


/* ── grid-3 ── */
.grid-3 {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 24px;
}

/* Resource Cards */
.resource-card {
  border-radius: var(--radius-lg);
  padding: 40px;
  display: flex;
  flex-direction: column;
  gap: 16px;
}
.resource-card--blue { background: var(--primary-light); }
.resource-card--purple { background: var(--purple-light); }
.resource-card-icon {
  width: 56px;
  height: 56px;
  background: var(--primary);
  border-radius: 14px;
  display: flex;
  align-items: center;
  justify-content: center;
}
.resource-card h3 { font-size: 22px; font-weight: 700; color: var(--text-dark); }
.resource-card p { font-size: 15px; color: var(--text-medium); }

/* ── Prayer Time Widget (v2 – reference match) ── */
.prayer-section { background: linear-gradient(135deg, #f5f9f0 0%, #e8f5e9 100%); padding: 48px 0; }
.prayer-widget {
  position: relative;
  display: grid;
  grid-template-columns: 240px 1fr 150px;
  align-items: center;
  background: #fff;
  border-radius: 20px;
  overflow: hidden;
  box-shadow: 0 6px 28px rgba(46,125,50,0.08);
  border: 1px solid #e0e0e0;
}

/* Left landscape */
.pw-landscape { align-self: stretch; overflow: hidden; }
.pw-landscape img { width: 100%; height: 100%; object-fit: cover; display: block; }

/* Center content */
.pw-center { padding: 24px 20px; display: flex; flex-direction: column; gap: 18px; }

.pw-top-row { display: flex; gap: 48px; align-items: flex-start; }

.pw-location {}
.pw-city { font-size: 22px; font-weight: 800; color: #333; line-height: 1.2; }
.pw-country { font-size: 14px; color: #888; font-weight: 400; }

.pw-heading {}
.pw-title { font-size: 24px; font-weight: 800; color: #333; line-height: 1.2; }
.pw-sunrise { display: flex; align-items: baseline; gap: 6px; margin-top: 2px; }
.pw-sunrise-time { font-size: 18px; font-weight: 800; color: #ff8800; }
.pw-sunrise-label { font-size: 14px; color: #888; font-weight: 400; }

/* Prayer items row */
.pw-prayers { display: flex; gap: 6px; }
.pw-prayer-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 2px;
  padding: 8px 14px;
  border-radius: 10px;
  transition: background 0.2s, box-shadow 0.2s;
  cursor: default;
}
.pw-prayer-item.next-prayer {
  background: #e8f5e9;
  box-shadow: inset 0 0 0 2px #4caf50;
}
.pw-prayer-svg { width: 32px; height: 32px; }
.pw-prayer-name { font-size: 11px; font-weight: 800; color: #388e3c; letter-spacing: 0.8px; text-transform: uppercase; }
.pw-prayer-time { font-size: 16px; font-weight: 800; color: #333; }
.pw-prayer-time::after { content: ''; }

/* Right corner decoration */
.pw-corner {
  position: absolute;
  top: -10px;
  right: -10px;
  width: 150px;
  height: 150px;
  pointer-events: none;
  opacity: 0.35;
}
.pw-corner img { width: 100%; height: 100%; object-fit: contain; }


/* ── Info section real image ── */
.info-img-wrap { border-radius: var(--radius-lg); overflow: hidden; box-shadow: var(--shadow-lg); }
.info-img { width: 100%; height: auto; display: block; }
/* Dark section */
.dark-section { background: var(--dark-section); }
.dark-card {
  background: rgba(255,255,255,0.06);
  border: 1px solid rgba(255,255,255,0.1);
  border-radius: var(--radius-lg);
  padding: 40px;
  color: #fff;
  transition: background 0.3s;
}
.dark-card:hover { background: rgba(255,255,255,0.1); }
.dark-card h3 { font-size: 22px; font-weight: 700; margin-bottom: 10px; }
.dark-card p { font-size: 15px; color: rgba(255,255,255,0.6); }

/* Blog layout */
.blog-grid-desktop {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 24px;
}
.blog-swipe-mobile { display: none; }
.swipe-hint { display: none; }



/* ── Scroll-reveal transitions ── */
.scroll-reveal {
  opacity: 0;
  transform: translateY(28px);
  transition: opacity 0.6s ease, transform 0.6s ease;
}
.scroll-reveal-child {
  opacity: 0;
  transform: translateY(24px);
  transition: opacity 0.55s ease calc(var(--stagger-delay, 0ms)), transform 0.55s ease calc(var(--stagger-delay, 0ms));
}
.is-visible .scroll-reveal,
.is-visible .scroll-reveal-child {
  opacity: 1;
  transform: translateY(0);
}

/* ── Mobile ── */
@media (max-width: 900px) {
  .info-grid { grid-template-columns: 1fr; gap: 32px; }
  .info-grid--reverse { direction: ltr; }
  /* image always first on mobile in both ruqyah and hijama sections */
  .info-grid .info-media { order: -1; }
  .info-grid .info-text  { order: 0; }
  .hijama-images { height: 220px; }
  .raki-grid-desktop { grid-template-columns: 1fr 1fr; }
  .video-grid { grid-template-columns: repeat(2, 1fr); }
  .grid-3 { grid-template-columns: 1fr 1fr; }
  .pkg-grid { grid-template-columns: repeat(2, 1fr); }
  .prayer-widget { grid-template-columns: 1fr; border-radius: 16px; }
  .pw-landscape { max-height: 120px; }
  .pw-landscape img { object-position: center 40%; }
  .pw-corner { width: 100px; height: 100px; opacity: 0.2; }
  .pw-center { padding: 20px 16px; }
  .pw-prayers { flex-wrap: nowrap; gap: 0; justify-content: space-between; }
  .pw-top-row { flex-direction: row; justify-content: space-between; align-items: flex-start; gap: 12px; }
  .pw-title { font-size: 20px; }
  .pw-city { font-size: 18px; }
  .pw-prayer-item { padding: 6px 8px; }
  .pw-prayer-svg { width: 28px; height: 28px; }
  .pw-prayer-name { font-size: 9px; letter-spacing: 0.5px; }
  .pw-prayer-time { font-size: 14px; }
}

@media (max-width: 768px) {
  .blog-grid-desktop { display: none; }
  .blog-swipe-mobile {
    display: flex;
    overflow-x: auto;
    scroll-snap-type: x mandatory;
    -webkit-overflow-scrolling: touch;
    gap: 14px;
    padding-bottom: 12px;
    scrollbar-width: none;
    -ms-overflow-style: none;
  }
  .blog-swipe-mobile::-webkit-scrollbar { display: none; }
  .blog-swipe-item { flex: 0 0 82vw; scroll-snap-align: start; }
  .swipe-hint { display: flex; justify-content: center; margin-top: 12px; }
  .swipe-hint span {
    font-size: 11px;
    color: var(--text-muted);
    background: var(--bg-light);
    padding: 4px 14px;
    border-radius: 20px;
    transition: opacity 0.6s ease;
  }
  .swipe-hint.faded span { opacity: 0; }

  .raki-grid-desktop { display: none; }
}

@media (max-width: 600px) {
  .video-grid { grid-template-columns: 1fr; }
  .grid-3 { grid-template-columns: 1fr; }
  .resource-card, .dark-card { padding: 24px; }
}

@media (max-width: 480px) {
  .info-grid { gap: 24px; }
  .hijama-images { height: 200px; }
  .pw-landscape { max-height: 100px; }
  .pw-center { padding: 16px 12px; gap: 12px; }
  .pw-prayer-item { padding: 5px 4px; min-width: 0; }
  .pw-prayer-svg { width: 24px; height: 24px; }
  .pw-prayer-name { font-size: 8px; }
  .pw-prayer-time { font-size: 12px; }
  .pw-title { font-size: 18px; }
  .pw-city { font-size: 16px; }
  .pw-sunrise-time { font-size: 15px; }
  .prayer-section { padding: 24px 0; }
}

@media (prefers-reduced-motion: reduce) {
  .scroll-reveal, .scroll-reveal-child { transition: none; }
}

/* FAQ Section */
.faq-list {
  max-width: 800px;
  margin: 0 auto;
  display: flex;
  flex-direction: column;
  gap: 12px;
}
.faq-item {
  background: var(--bg-white);
  border: 1px solid var(--border);
  border-radius: var(--radius-md);
  overflow: hidden;
  transition: box-shadow 0.2s ease;
}
.faq-item:hover {
  box-shadow: var(--shadow-md);
}
.faq-question {
  padding: 18px 24px;
  font-size: 16px;
  font-weight: 600;
  color: var(--text-dark);
  cursor: pointer;
  list-style: none;
  display: flex;
  justify-content: space-between;
  align-items: center;
  transition: background 0.2s ease;
}
.faq-question::-webkit-details-marker { display: none; }
.faq-question::after {
  content: '+';
  font-size: 22px;
  font-weight: 300;
  color: var(--primary);
  transition: transform 0.2s ease;
  flex-shrink: 0;
  margin-left: 16px;
}
.faq-item[open] .faq-question::after {
  content: '−';
}
.faq-item[open] .faq-question {
  background: var(--primary-light);
  color: var(--primary-dark);
}
.faq-answer {
  padding: 16px 24px 20px;
  font-size: 15px;
  line-height: 1.75;
  color: var(--text-body);
  border-top: 1px solid var(--border);
}

/* Bilingual Definition Section (for AI search) */
.definition-section {
  padding: 48px 0;
}
.definition-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 32px;
}
.definition-bn,
.definition-en {
  background: var(--bg-white);
  border-radius: var(--radius-lg);
  padding: 28px;
  border: 1px solid var(--border);
}
.definition-bn {
  border-left: 4px solid var(--primary);
}
.definition-en {
  border-left: 4px solid #3B82F6;
}
.definition-video {
  background: var(--bg-white);
  border-radius: var(--radius-lg);
  padding: 0;
  border: 1px solid var(--border);
  border-left: 4px solid var(--primary);
  overflow: hidden;
}
.definition-video-embed {
  position: relative;
  width: 100%;
  aspect-ratio: 16 / 9;
  overflow: hidden;
  border-radius: 0;
  background: #000;
}
.definition-video-embed iframe {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  border: 0;
}
.definition-bn h2,
.definition-en h2 {
  font-size: 1.3rem;
  font-weight: 700;
  margin-bottom: 16px;
  color: var(--text-dark);
}
.definition-text {
  font-size: 15px;
  line-height: 1.8;
  color: var(--text-body);
}

@media (max-width: 768px) {
  .definition-grid {
    grid-template-columns: 1fr;
    gap: 20px;
  }
}
</style>
