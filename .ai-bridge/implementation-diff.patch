diff --git a/.ai-bridge/current-plan.md b/.ai-bridge/current-plan.md
index 2296e70..de62c4f 100644
--- a/.ai-bridge/current-plan.md
+++ b/.ai-bridge/current-plan.md
@@ -1,17 +1,31 @@
-# Homepage services video section
+# Homepage hero test cases
 
-Updated: 2026-07-08T12:59:00+06:00
+Updated: 2026-07-09T04:38:22.544Z
 Workspace: /Users/rahmatullahzisan/Desktop/Dev/ruqyah-healing
+Target agent: Codex (codex)
 
-Task: Add the six YouTube videos shared by Rajin Saleh under the homepage services section.
+## Plan
+
+Task: Add regression tests for the homepage hero readability/slider changes.
 
 Plan:
-1. Locate homepage services and existing video section.
-2. Replace placeholder video data with the six real YouTube IDs and labels.
-3. Move/enable the video section directly below “আমাদের সেবাসমূহ”.
-4. Keep responsive styling suitable for regular YouTube videos and Shorts.
-5. Run build verification and update session memory files.
+1. Add a focused Node test file under `tests/` that reads `src/components/Hero.astro` as source text.
+2. Assert the hero has the new slider container, default `data-active-slide`, four slide image layers, and one CSS mapping per slide.
+3. Assert the typewriter script updates `data-active-slide`, guards against duplicate initialization, and handles reduced motion by showing static text.
+4. Assert readability/contrast tokens remain present: strong overlay, solid hero-card background, text shadows, mobile overlay, and corrected Bengali typo.
+5. Run the new focused test and `npm run build`.
+6. Update active context and Codex status with files touched and verification.
 
 Constraints:
-- Do not deploy unless explicitly requested.
-- Preserve existing unrelated uncommitted work.
+- Keep tests lightweight and compatible with the existing `node --test tests/*.test.mjs` setup.
+- Do not deploy.
+- Preserve unrelated working tree changes.
+
+## Implementation contract
+
+- Work from this plan in small, reviewable steps.
+- Keep edits scoped to the requested task and existing project conventions.
+- Run focused verification before handing work back.
+- Update .ai-bridge/agent-status.md with files touched, checks run, results, blockers, and review notes.
+- Save the final review diff to .ai-bridge/implementation-diff.patch when practical.
+- Append notable execution events to .ai-bridge/execution-log.jsonl when the implementation agent supports logging.
diff --git a/src/components/Hero.astro b/src/components/Hero.astro
index ae0e49b..b26d634 100644
--- a/src/components/Hero.astro
+++ b/src/components/Hero.astro
@@ -1,7 +1,13 @@
 ---
 import { socialLinks } from '../data/clinic.js';
 ---
-<section class="hero">
+<section class="hero" data-active-slide="0" aria-label="রুকইয়াহ হিলিং পরিচিতি">
+  <div class="hero-bg-slider" aria-hidden="true">
+    <div class="hero-bg-slide" style="--hero-slide-image: url('/images/hero-ruqyah-healing-bg.webp');"></div>
+    <div class="hero-bg-slide" style="--hero-slide-image: url('/images/prayer-landscape.webp');"></div>
+    <div class="hero-bg-slide" style="--hero-slide-image: url('/images/ruqyah-info.webp');"></div>
+    <div class="hero-bg-slide" style="--hero-slide-image: url('/images/hijama-main.webp');"></div>
+  </div>
   <div class="container hero-content">
     <div class="hero-copy">
       <div class="hero-badge">
@@ -13,7 +19,7 @@ import { socialLinks } from '../data/clinic.js';
 
       <h1 class="hero-title">রুকইয়াহ হিলিং — ইসলামী রুকইয়াহ চিকিৎসা</h1>
       <p class="hero-subtitle" data-typewriter='["পরিবারে অশান্তি বা কলহ?", "বিয়েতে বাধা বা সন্তান না হওয়া?", "ব্যবসায়ে মন্দা বা সৌভাগ্যে বাধা?", "অসহ্য শারীরিক যন্ত্রণা?"]'></p>
-      <p class="hero-desc">জিন, জাদু ও বদনজরের কারণে সৃষ্ট সমস্যার সমাধান — কুরআন ও সুন্নাহ ভিত্তিক ১০০% হালাল পদ্ধতে</p>
+      <p class="hero-desc">জিন, জাদু ও বদনজরের কারণে সৃষ্ট সমস্যার সমাধান — কুরআন ও সুন্নাহ ভিত্তিক ১০০% হালাল পদ্ধতিতে</p>
 
       <div class="hero-actions">
         <a href="/appointment" class="btn btn-hero-primary">
@@ -77,8 +83,10 @@ import { socialLinks } from '../data/clinic.js';
 
   function initTypewriter() {
     const el = document.querySelector('[data-typewriter]');
-    if (!el) return;
+    if (!el || el.dataset.typewriterStarted === 'true') return;
+    el.dataset.typewriterStarted = 'true';
 
+    const hero = el.closest('.hero');
     const prefersReduced = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
     const typingSpeed = prefersReduced ? 0 : 80;
     const deletingSpeed = prefersReduced ? 0 : 40;
@@ -92,9 +100,20 @@ import { socialLinks } from '../data/clinic.js';
       texts = [raw];
     }
     if (!texts.length) texts = [raw];
-    if (!el) return;
+
+    function updateHeroSlide(index) {
+      if (!hero) return;
+      hero.setAttribute('data-active-slide', String(index % texts.length));
+    }
 
     let textIndex = 0;
+    updateHeroSlide(textIndex);
+
+    if (prefersReduced) {
+      el.textContent = texts[0];
+      return;
+    }
+
     let isDeleting = false;
     let isPaused = false;
 
@@ -124,6 +143,7 @@ import { socialLinks } from '../data/clinic.js';
         } else {
           isDeleting = false;
           textIndex = (textIndex + 1) % texts.length;
+          updateHeroSlide(textIndex);
           setTimeout(type, 500);
         }
       }
@@ -140,25 +160,46 @@ import { socialLinks } from '../data/clinic.js';
 <style>
 .hero {
   position: relative;
-  min-height: 560px;
+  min-height: 600px;
   display: flex;
   align-items: center;
-  background: url('/images/hero-ruqyah-healing-bg.webp') center / cover no-repeat;
+  background: #063d31;
   overflow: hidden;
+  isolation: isolate;
+}
+
+.hero-bg-slider {
+  position: absolute;
+  inset: 0;
+  z-index: 0;
+}
+
+.hero-bg-slide {
+  position: absolute;
+  inset: 0;
+  background-image: var(--hero-slide-image);
+  background-position: center;
+  background-size: cover;
+  opacity: 0;
+  transform: scale(1.045);
+  transition: opacity 700ms ease, transform 1400ms ease;
+}
+
+.hero[data-active-slide="0"] .hero-bg-slide:nth-child(1),
+.hero[data-active-slide="1"] .hero-bg-slide:nth-child(2),
+.hero[data-active-slide="2"] .hero-bg-slide:nth-child(3),
+.hero[data-active-slide="3"] .hero-bg-slide:nth-child(4) {
+  opacity: 1;
+  transform: scale(1);
 }
 
 .hero::before {
   content: '';
   position: absolute;
-  inset: 0 auto 0 0;
-  width: min(52%, 700px);
-  background: linear-gradient(
-    90deg,
-    rgba(3, 78, 58, 0.20) 0%,
-    rgba(3, 78, 58, 0.11) 52%,
-    rgba(3, 78, 58, 0.04) 78%,
-    rgba(3, 78, 58, 0) 100%
-  );
+  inset: 0;
+  background:
+    linear-gradient(90deg, rgba(2, 34, 27, 0.94) 0%, rgba(3, 62, 48, 0.86) 42%, rgba(4, 63, 49, 0.58) 72%, rgba(3, 42, 34, 0.45) 100%),
+    linear-gradient(180deg, rgba(0, 0, 0, 0.18) 0%, rgba(0, 0, 0, 0.34) 100%);
   pointer-events: none;
   z-index: 1;
 }
@@ -168,13 +209,13 @@ import { socialLinks } from '../data/clinic.js';
   z-index: 2;
   display: block;
   text-align: left;
-  width: min(100%, 680px);
-  max-width: 680px;
+  width: min(100%, 700px);
+  max-width: 700px;
   min-width: 0;
   margin-right: auto;
   box-sizing: border-box;
-  padding-top: 80px;
-  padding-bottom: 80px;
+  padding-top: 88px;
+  padding-bottom: 88px;
   color: #fff;
 }
 
@@ -182,15 +223,15 @@ import { socialLinks } from '../data/clinic.js';
   position: relative;
   z-index: 2;
   width: 100%;
-  max-width: 680px;
+  max-width: 700px;
   min-width: 0;
-  padding: 28px 30px;
-  border-radius: 26px;
-  background: linear-gradient(135deg, rgba(3, 78, 58, 0.22) 0%, rgba(255, 255, 255, 0.08) 100%);
-  border: 1px solid rgba(255, 255, 255, 0.16);
-  box-shadow: 0 18px 42px rgba(0, 0, 0, 0.08);
-  backdrop-filter: blur(4px);
-  -webkit-backdrop-filter: blur(4px);
+  padding: 32px;
+  border-radius: 28px;
+  background: linear-gradient(135deg, rgba(3, 56, 44, 0.92) 0%, rgba(7, 89, 66, 0.82) 100%);
+  border: 1px solid rgba(255, 255, 255, 0.26);
+  box-shadow: 0 22px 55px rgba(0, 0, 0, 0.32);
+  backdrop-filter: blur(12px);
+  -webkit-backdrop-filter: blur(12px);
 }
 
 :global(.home-page .hero .hero-content),
@@ -209,42 +250,48 @@ import { socialLinks } from '../data/clinic.js';
   display: inline-flex;
   align-items: center;
   gap: 6px;
-  background: rgba(255,255,255,0.15);
-  backdrop-filter: blur(8px);
-  border: 1px solid rgba(255,255,255,0.25);
-  color: rgba(255,255,255,0.95);
+  background: rgba(6, 95, 70, 0.82);
+  backdrop-filter: blur(10px);
+  border: 1px solid rgba(255,255,255,0.34);
+  color: #f7fffb;
   font-size: 13px;
-  font-weight: 600;
-  padding: 6px 16px;
+  font-weight: 700;
+  padding: 7px 16px;
   border-radius: 20px;
   margin-bottom: 22px;
   letter-spacing: 0.3px;
+  text-shadow: 0 1px 2px rgba(0,0,0,0.35);
 }
 
 .hero-title {
   font-family: 'Lora', 'Hind Siliguri', serif;
   font-size: clamp(22px, 3.5vw, 34px);
-  font-weight: 700;
+  font-weight: 800;
   margin-bottom: 14px;
+  color: #ffffff;
   letter-spacing: -0.5px;
   line-height: 1.15;
   overflow-wrap: break-word;
+  text-shadow: 0 2px 8px rgba(0,0,0,0.48);
 }
 
 .hero-subtitle {
   font-size: clamp(24px, 4.8vw, 38px);
-  font-weight: 600;
+  font-weight: 800;
   margin-bottom: 14px;
-  color: rgba(255,255,255,0.92);
+  color: #f6fff9;
   min-height: 1.5em;
+  text-shadow: 0 2px 10px rgba(0,0,0,0.52);
 }
 
 .hero-desc {
   font-size: 16px;
-  color: rgba(255,255,255,0.78);
-  max-width: 580px;
+  font-weight: 600;
+  color: #edfdf5;
+  max-width: 600px;
   margin: 0 0 36px;
   line-height: 1.75;
+  text-shadow: 0 1px 5px rgba(0,0,0,0.42);
 }
 
 .hero-actions {
@@ -366,11 +413,12 @@ import { socialLinks } from '../data/clinic.js';
   gap: 32px;
   max-width: 100%;
   box-sizing: border-box;
-  background: rgba(255,255,255,0.1);
-  backdrop-filter: blur(8px);
-  border: 1px solid rgba(255,255,255,0.2);
+  background: rgba(2, 44, 34, 0.74);
+  backdrop-filter: blur(10px);
+  border: 1px solid rgba(255,255,255,0.25);
   border-radius: 16px;
   padding: 16px 32px;
+  box-shadow: inset 0 1px 0 rgba(255,255,255,0.12);
 }
 .hero-stat {
   display: flex;
@@ -389,7 +437,7 @@ import { socialLinks } from '../data/clinic.js';
 }
 .hero-stat-label {
   font-size: 12px;
-  color: rgba(255,255,255,0.72);
+  color: rgba(255,255,255,0.88);
   white-space: nowrap;
 }
 .hero-stat-divider {
@@ -404,14 +452,9 @@ import { socialLinks } from '../data/clinic.js';
     align-items: flex-start;
   }
   .hero::before {
-    width: 100%;
-    background: linear-gradient(
-      180deg,
-      rgba(3, 78, 58, 0.16) 0%,
-      rgba(3, 78, 58, 0.10) 48%,
-      rgba(3, 78, 58, 0.04) 76%,
-      rgba(3, 78, 58, 0) 100%
-    );
+    background:
+      linear-gradient(180deg, rgba(2, 34, 27, 0.92) 0%, rgba(3, 60, 47, 0.82) 52%, rgba(2, 34, 27, 0.76) 100%),
+      linear-gradient(90deg, rgba(0,0,0,0.28), rgba(0,0,0,0.18));
   }
   .hero-content {
     width: 100%;
@@ -426,9 +469,9 @@ import { socialLinks } from '../data/clinic.js';
     max-width: 100%;
     margin-left: auto;
     margin-right: auto;
-    padding: 20px 16px;
+    padding: 22px 16px;
     border-radius: 20px;
-    background: linear-gradient(180deg, rgba(3, 78, 58, 0.18) 0%, rgba(255, 255, 255, 0.07) 100%);
+    background: linear-gradient(180deg, rgba(2, 50, 39, 0.94) 0%, rgba(5, 82, 61, 0.88) 100%);
   }
   .hero-desc {
     max-width: 100%;
@@ -493,6 +536,9 @@ import { socialLinks } from '../data/clinic.js';
 }
 
 @media (prefers-reduced-motion: reduce) {
-  .hero-orb { animation: none; }
+  .hero-bg-slide {
+    transition: none;
+    transform: none;
+  }
 }
 </style>
